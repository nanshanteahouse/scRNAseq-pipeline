# ═══════════════════════════════════════════════════════════════════════
# 通用 scRNA-seq 管线依赖
# ═══════════════════════════════════════════════════════════════════════
# 安装: pip install -r requirements.txt

# ── 核心 ────────────────────────────────────────────────────────────
scanpy>=1.10.0              # 单细胞分析主框架
anndata>=0.10.0             # AnnData 数据结构

# ── 双细胞检测 ─────────────────────────────────────────────────────
scrublet>=0.2.3             # Scrublet 双细胞检测

# ── 批次校正 ───────────────────────────────────────────────────────
harmony-pytorch>=0.1.8      # PyTorch 版 Harmony（免编译）

# ── 矩阵与科学计算 ─────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
joblib>=1.3.0               # 并行计算（Scrublet 多样本并行）

# ── 可视化 ─────────────────────────────────────────────────────────
matplotlib>=3.7.0
leidenalg>=0.10.0           # Leiden 聚类算法（可选，可用 igraph 替代）

# ── 富集分析 (Step 09) ─────────────────────────────────────────────
gseapy>=1.1.0               # GSEApy: GO/KEGG/Reactome 通路富集
# 注意: gseapy > 0.11.0 需 Rust 编译器。如果 pip 安装失败:
#   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# ── WSL 兼容 (仅 Linux/WSL 需要) ───────────────────────────────────
# 如果在 WSL 上使用 /mnt/d 等 Windows 挂载路径，
# 需要在运行前设置:
#   export HDF5_USE_FILE_LOCKING=FALSE
# 或在 Python 中:
#   import os; os.environ['HDF5_USE_FILE_LOCKING'] = 'FALSE'
